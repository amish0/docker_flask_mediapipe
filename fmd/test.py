print("amish")
import time
# add the following line to the end of the file
time.sleep(10)
# This will keep the server running for 1000 seconds
# You can change the time as needed
# Now, you can run the server using the following command:
def add(a,b):
    return a+b

print(add(2,3))